# Migrations

New database changes go here as `YYYYMMDDHHMMSS_short_description.sql`.

Only real changes belong in this folder. See `../README.md` for the workflow and for the baseline SQL files that predate this folder.
